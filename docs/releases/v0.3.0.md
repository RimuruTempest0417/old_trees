# v0.3.0 — 森林背景圖片與模糊效果

> ⚠️ **此版本的 `.env.example` 曾被填入真實的 Supabase 網址與金鑰**，請勿使用該檔案的內容。
> 該檔案已於 v0.3.1 改回佔位符，請改用 v0.3.1 或之後的版本；若你曾取得此版檔案，請立即輪換金鑰。

## 更新內容

- 複製森林背景圖片（`pngtree-cartoon-forest-background-picture-image_15950274.jpg`）為 `public/photos/forest-bg.jpg`。
- `public/css/style.css` 以 `body::before` 承載背景圖片，套用模糊效果。
- 深色模式為背景圖層額外疊加底色，避免深色版面露出亮色背景。

## 已知問題（已於 v0.3.1 修正）

- `.env.example` 含真實憑證。
- 總覽頁第 8 張統計卡顯示系統內部資訊（資料來源），且版面會落單。
- 環境變數為 `.env.example` 佔位符時，API 會先等 DNS 逾時再回 500。
