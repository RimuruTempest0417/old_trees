# v0.6.6 — 健康檢查誤報修正：你的資料庫其實已經升級完成了

## 一、你的 `init.sql` 這次跑成功了

截圖顯示檔案執行到最後（`setval` 後 `commit;`，第 1812 行），所以 Supabase 那邊的
資料表、欄位、檢視表、函式與 658 筆資料都已補齊。剩下的三個「缺少的項目」
**不是資料庫的問題，是我的健康檢查清單寫錯了**：

| 畫面顯示缺少 | 實際情形 |
| --- | --- |
| `v_trees.photo_url` | 檢視表把該欄命名為 `tree_photo`（另有 `site_photo`、`species_photo`），根本沒有叫 `photo_url` 的欄位 |
| `trees.species` | `trees` 表用的是 `species_id`（外鍵）；品種名稱只出現在檢視表 `v_trees.species` |
| `routes.slug` | `routes` 表用的是 `code`（唯一鍵）；`slug` 是 `conservation_topics` 的欄位 |

也就是說：探測清單寫成了「憑印象的欄位名」，與 `schema.sql` 的真實欄位脫節，
於是資料庫明明已經完整，畫面卻一直喊「需要升級」。

## 二、修正

1. `lib/repo.js` 的 `SCHEMA_PROBES` 改為真實存在的欄位：
   - `v_trees(tree_no, official_no, tree_geo_precision, geo_precision, tree_photo, official_age_years)`
   - `trees(tree_no, species_id, age_years, height_m, lat, lon)`
   - `routes(code, site_names)`
2. `/api/health` 新增 `version` 欄位（目前 `v0.6.6`），一眼就能確認線上跑的是哪一版。
3. **新增兩項防止再犯的測試**（`tests/sql.test.js`）：
   - 「健康檢查的探測清單與綱要一致」：用 PGlite 建好真實資料庫後，逐一執行每個探測
     查詢，任何欄位／函式不存在就失敗。
   - 「舊資料庫（缺欄位／缺表／舊 CHECK）跑完 init.sql 後，健康檢查每一項都通過」：
     先故意拿掉 `trees.official_no`、`field_records` 表、`v_trees` 檢視表、換回舊版 CHECK，
     再跑 schema ＋ seed，然後驗證全部探測項目與 `official` 座標資料都正常。

這兩項測試的價值當場就体现了：修正過程中我一度把探測欄位改回舊值，測試立刻失敗並指出
`column "photo_url" does not exist`，才沒有把錯誤的清單再送出去一次。

## 三、驗證

- `npm run check`：44 個檔案語法全過。
- `npm test`：**10 組、133 項全過**（新增 2 項）。
- 舊庫模擬（PGlite）：升級後 6 個探測項全通過、3 個 `rpc_*` 函式存在、
  `sites.geo_precision` 允許 `official` 且 127 筆官方座標地點寫得進去。

## 四、你要做的

**重新整理頁面即可**（Vercel 會在推送後自動重新部署；部署完成後橫幅就會消失）。
若仍看到橫幅，請把 `/api/health` 的內容貼給我——裡面的 `version` 會顯示線上實際版本。
